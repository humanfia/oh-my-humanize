# Worker

Item key: {{itemKey}}
Item: {{item}}

Process the item and write a result to `/pool/results/<itemKey>` with a summary.
If the item key is `task-1`, include a field `expand: true` to request expansion.
